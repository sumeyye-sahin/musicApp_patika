import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  container: {
    height: 40,
    backgroundColor: '#e0e0e0',
    paddingLeft: 5,
    borderRadius: 5,
  },
});
